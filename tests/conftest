import os
import sys

# Allow `import core.xxx` / `import modes.xxx` when running `pytest` from
# the project root or from within tests/.
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))
